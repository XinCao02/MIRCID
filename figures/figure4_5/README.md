# Figure 4.5

Run `make_figure4_5.py` after installing MIRCID and setting `MIRCID_DATA_ROOT`. Panels cover matched embedding controls, held-out CCA modes, linear/RBF CKA, ridge predictability and paired downstream changes. Figure 4's selected test metrics are stored once in `figure_source_data/figure4/`; derived panel-e values are stored here.


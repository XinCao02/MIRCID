# Figure 2

`figure2.png` is the current composite candidate. `make_figure2_data_panels.py` reconstructs the quantitative model-benchmark and observed-versus-predicted PCA panels from real source data. No mock 1,298-miRNA error bars are included. The model architecture/workflow artwork is retained only in the composite and should eventually be redrawn from a vector source.


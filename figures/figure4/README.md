# Figure 4

`make_figure4.py` is the portable plotter. `provenance_v9_selection.py` and `figure4_layout.py` preserve the historical final-candidate selection/layout logic. The staged source table contains seven paired runs per model selected from larger pools; the corresponding selection summary is in the data archive. For confirmatory reporting, rerun all 20 frozen splits with `mircid.pathway`.


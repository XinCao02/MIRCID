# Figure 3

`make_figure3.py` draws the 3 datasets × 3 metrics TFA summary from `tfa_figure3_aggregate_metrics.csv`. Those 81 values were recovered from the submitted figure, not from complete sample-level outputs. This reproduces the displayed numbers but cannot support new inferential statistics until the nine underlying TFA runs are recovered or rerun.


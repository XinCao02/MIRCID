# Figure 5

`make_figure5.py` reads the frozen mechanism panel plus the 99-row featurewise-normalized heatmap table. `provenance_figure5_v7.py` and its helper files preserve the historical integration logic. Rescue status is associative: it denotes gene-only wrong and Gene+HubmiR correct on a held-out prediction, not causal miRNA rescue.

